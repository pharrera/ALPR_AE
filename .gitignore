# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# ── Old Kaggle/OpenALPR dataset (replaced by UFPR-ALPR) ─────────────────────
data/train/
data/valid/
data/test/
data/plates/
data/openalpr/
data/data.yaml

# ── UFPR-ALPR raw dataset (4,500 PNGs — too large for git) ──────────────────
# Regenerate with: download dataset, run prepare_ufpr_yolo.py
data/UFPR-ALPR dataset/

# ── UFPR YOLO split (generated by prepare_ufpr_yolo.py) ─────────────────────
# Images and labels are regenerated; plate_gt.json IS tracked (see below)
data/ufpr_yolo/train/
data/ufpr_yolo/valid/
data/ufpr_yolo/test/
data/CCPD2019
# ── CCPD dataset (if downloaded) ────────────────────────────────────────────
data/ccpd/
data/ccpd_green/

# ── Model weights (store on server / Google Drive) ───────────────────────────
*.pt
*.pth
results/detection/
results/autoencoder/
runs/

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
.vscode/

# ── Outputs (regenerated by scripts) ─────────────────────────────────────────
results/experiment/plots/

# ── Explicitly keep these generated-but-small files ──────────────────────────
# Ground truth plate text mapping — needed for OCR evaluation
!data/ufpr_yolo/plate_gt.json
# Experiment results JSON
!results/experiment/experiment_results.json
